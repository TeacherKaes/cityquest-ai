'use client';
import { useState } from "react";
import QRCode from "qrcode";
import Link from "next/link";
import { listEditions, getEdition } from "../../lib/editionRegistry";
export default function TeacherPage(){const all=listEditions();const[step,setStep]=useState(1);const[editionId,setEditionId]=useState("london");const[level,setLevel]=useState("A2/B1");const[duration,setDuration]=useState(20);const[teamName,setTeamName]=useState("Team 1");const[sessionLink,setSessionLink]=useState("");const[qr,setQr]=useState("");const[status,setStatus]=useState("");const edition=getEdition(editionId);async function create(){setStatus("Quest wird vorbereitet...");const res=await fetch("/api/sessions/create",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({editionId,teamName,level,duration})});const data=await res.json();const base=window.location.origin;let link;if(data.mode==="supabase"&&data.sessionId){link=`${base}/play/${data.sessionId}`}else{localStorage.setItem("cityquest-alpha-session",JSON.stringify(data.session));link=`${base}/play`}setSessionLink(link);setQr(await QRCode.toDataURL(link));setStatus(data.mode==="supabase"?"Online-Session erstellt.":"Demo-Session erstellt. Für echte Mehrgeräte-Nutzung Supabase aktivieren.");setStep(3)}return <><header className="topbar"><div className="brand"><div className="logo">🧑‍🏫</div><div><h1>Quest erstellen</h1><span>3 Schritte bis zum QR-Code</span></div></div><Link href="/"><button className="ghost">Home</button></Link></header><main><div className="card"><div className="step"><div className="stepnum">1</div><div><b>Edition</b><br/>Wähle den Lernort.</div></div><div className="step"><div className="stepnum">2</div><div><b>Einstellungen</b><br/>Niveau, Dauer und Team.</div></div><div className="step"><div className="stepnum">3</div><div><b>QR-Code</b><br/>An Schülerinnen und Schüler geben.</div></div></div>{step===1&&<div className="card" style={{marginTop:18}}><h2>1. Edition wählen</h2><div className="grid">{all.map(e=><div key={e.id} className="quest-card" onClick={()=>setEditionId(e.id)} style={{borderColor:editionId===e.id?"rgba(255,209,102,.8)":"var(--line)",cursor:"pointer"}}><h3>{e.title}</h3><p>{e.description}</p><span className="tag gold">{e.subtitle}</span></div>)}</div><button onClick={()=>setStep(2)}>Weiter</button></div>}{step===2&&<div className="card" style={{marginTop:18}}><h2>2. Einstellungen</h2><div className="grid"><div><label>Teamname</label><input value={teamName} onChange={e=>setTeamName(e.target.value)}/></div><div><label>Niveau</label><select value={level} onChange={e=>setLevel(e.target.value)}><option>A1/A2</option><option>A2/B1</option><option>B1</option></select></div><div><label>Dauer</label><select value={duration} onChange={e=>setDuration(Number(e.target.value))}><option value={20}>20 Minuten</option><option value={45}>45 Minuten</option><option value={90}>90 Minuten</option></select></div></div><div className="actions"><button className="ghost" onClick={()=>setStep(1)}>Zurück</button><button onClick={create}>QR-Code erzeugen</button></div></div>}{step===3&&<div className="card" style={{marginTop:18,textAlign:"center"}}><h2>3. QR-Code zeigen</h2><p>{status}</p><p style={{wordBreak:"break-all"}}>{sessionLink}</p>{qr&&<div className="qr"><img alt="QR Code" src={qr} width="220" height="220"/></div>}<div className="actions" style={{justifyContent:"center"}}><a href={sessionLink}><button>Spiel öffnen</button></a><button className="ghost" onClick={()=>setStep(1)}>Neue Quest</button></div></div>}</main></>;}
